namespace MusicPracticeMonitor
{
    /// <summary>
    /// Defines the overall session status.
    /// </summary>
    public enum SessionStatus
    {
        NotStarted,
        Running,
        Paused,
        Stopped
    }
}
